// Aquí irían funciones auxiliares si las necesitáramos, por ahora está vacío.
// Por ejemplo:
// export const formatCurrency = (amount) => `$${amount.toFixed(2)}`;
// export const validateEmail = (email) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);